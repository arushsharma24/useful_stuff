#include <bits/stdc++.h>
using namespace std;
#define ll long long

// solution using the formula that grey (n) = n^(n>>1);

int main(){
    ll n; cin>>n;
    for(ll i = 0; i < (1<<n); i++){ // (1<<n) is the same as pow(2,n)
        
    }
}